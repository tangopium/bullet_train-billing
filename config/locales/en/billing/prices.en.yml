en:
  billing/prices:
    models:
      memberships:
        quantifier: Seat
        per: Per Seat
    intervals:
      month:
        description: Monthly
        per: Per Month
      3_months:
        description: Quarterly
        per: Per Quarter
      year:
        description: Annually
        per: Per Year
